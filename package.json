{
  "name": "yaliquankai",
  "version": "1.0.0",
  "private": true,
  "description": "Quality checks for the Yaliquankai WeChat mini program",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "test": "node --test",
    "check": "npm run lint && npm test"
  },
  "engines": {
    "node": "^20.19.0 || ^22.13.0 || >=24"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "eslint": "10.9.1"
  }
}
