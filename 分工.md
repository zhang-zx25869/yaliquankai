# 项目分工与现状说明

> 面向三位开发者。看完这份文档 + `README.md`，你应该清楚：项目是什么、已经有什么、你负责什么、从哪里开始动手。

## 一、项目一句话

「雅力全开」小程序：新雅书院体育部的**赛事跟场与后勤管理平台**。队长发布赛程 → 微信卡片转发经理人 → 经理人确认/没空 → 四色看板（黄待确认/绿已确认/红求助中/橙待结算）全局监控 → 赛后填比分归档、看板单元格销号。

完整业务逻辑见 `../需求文档.md`（含 19 个用例的分析模型，每个用例写明了界面类→控制类→实体类的调用链——**这就是你的开发说明书**）。

## 二、技术架构（三层）

| 层 | 位置 | 职责 |
|---|---|---|
| 界面类（前端页面） | `miniprogram/pages/` | 展示与交互，改完点「编译」即生效 |
| 控制类（云函数） | `cloudfunctions/` | 业务规则、身份校验、状态机流转；**改完必须右键→上传并部署** |
| 实体类（云数据库） | 云开发控制台 5 个集合 | 数据存储；权限已设为前端全禁，只走云函数 |

前端调用云端统一用 `utils/call.js` 的 `call('函数名', { action, ...参数 })`；状态与角色常量统一引用 `utils/status.js`，**不要自己另写魔法字符串**。

## 三、已打好的地基（2025-xx-xx 由发起者完成）

以下是已经完成、可直接使用的部分：

1. **云开发环境**：环境 ID `yaliquankai-d2g2kt52247d144a8`（已写入 `app.js`/`envList.js`），个人版套餐，5 个数据库集合已建好且权限已锁定（前端禁读禁写，全部走云函数）；
2. **`cloudfunctions/AuthManager/`（云函数，已写完）**：身份认证。`autoLogin`（openid 静默登录，app.js 启动时自动调用）+ `bindIdentity`（一次性激活码绑定角色）。**三人都需要它来获取身份，但不需要改它**；
3. **`miniprogram/pages/profile/`（我的页，已写完）**：身份显示 + 激活码绑定输入框，可直接测试；
4. **`miniprogram/components/match-detail-card/`（共用组件，已写完）**：比赛详情卡片，下滑展开，支持状态色点/比分/云盘链接/按角色显示按钮。首页、队伍页、看板页都复用它，**用 `<match-detail-card>` 标签即可**（已在 app.json 全局注册）；
5. **8 个页面骨架**：四 Tab（首页 index / 队伍 team / 看板 dashboard / 我的 profile）+ 4 个功能页（respond 响应 / rescue 救场 / archive 归档 / schedule-form 赛程表单）——**全部是占位空页，等你填充**；
6. **`utils/status.js`**：全项目状态机定义（7 种单元格状态、4 种角色），前端云端共用；
7. **`utils/call.js`**：统一云函数调用封装；
8. **模板垃圾已清理**：示例页、示例组件、示例图片已删；
9. **Git + README**：协作规范、部署清单在 `README.md`。

## 四、三人分工

每人负责**一条纵向业务线**（自己的页面 + 自己的云函数），互不阻塞。

### A 同学 · 赛程线（队长侧）

**目标：队长能发布/修改/取消比赛，游客能在首页看到赛程。**

| 要做 | 位置 | 对应用例 |
|---|---|---|
| C2 赛程管理云函数 | `cloudfunctions/ScheduleManager/`（新建） | 用例3/4/5 |
| B9 赛程表单页：录入（项目/对阵/时间/地点/勾选后勤需求）、修改、取消比赛、TBD 勾选 | `pages/schedule-form/` | 用例3/5 |
| B2 首页公共日历：日历视图 + 比赛卡片列表（复用 match-detail-card） | `pages/index/` | 用例16/17 |

**云函数 action 清单**：`saveMatch`（含三条规则：改时间/地点→清空确认重置黄色；距开赛<48h 修改→直接拉红；勾选 TBD→灰色挂起，见需求文档用例3）、`getShareCard`、`cancelMatch`。

**状态机写入规则**（详情看 `utils/status.js` 和需求文档用例3）：新建赛程 `cellStatus = 'pending'`（黄）。

**关键提示**：队长入口按钮放在首页（队长角色才显示），跳转 schedule-form 页。

### B 同学（发起者）· 跟场线（部员侧）——逻辑最重的一条线

**目标：经理人点微信卡片直达响应页，确认/没空/救场全流程跑通，看板颜色随之流转。**

| 要做 | 位置 | 对应用例 |
|---|---|---|
| C3 跟场任务云函数 | `cloudfunctions/DutyManager/`（新建） | 用例6/7/8/9/10 |
| B6 响应页：顶部比赛信息 + 本队跟场统计 + 确认/没空/取消按钮 | `pages/respond/` | 用例6/7/10 |
| B7 救场页：比赛信息 + 接单按钮 | `pages/rescue/` | 用例9 |

**云函数 action 清单**：`getTeamStats`、`confirmDuty`、`declineDuty`、`generateHelpCard`、`rescueDuty`、`cancelMyDuty`。

**核心业务规则**（必须严格实现，全部在需求文档用例7/8/9/10）：
- 多经理人队伍：**1 人确认即绿**；全员没空才红 + 解锁求助卡片；
- 救场只能通过点大群求助卡片进入 rescue 页接单，看板不能直接接单；
- 距开赛 <48h 取消跟场：弹安全锁强提醒二次确认；
- 取消后释放名额回黄色，别人可重新确认。

**分享卡片直达**：卡片路径带 `matchId` 参数，`respond` 页 `onLoad(options)` 取参数加载对应比赛。

### C 同学 · 看板与档案线（运营者+归档侧）

**目标：四色看板正确展示与流转，运营者能裁决，赛后归档销号闭环。**

| 要做 | 位置 | 对应用例 |
|---|---|---|
| C5 看板云函数 | `cloudfunctions/DashboardManager/`（新建） | 用例13/14/15 |
| B4 四色看板页：二维表（纵轴队伍×横轴场次），点单元格展开卡片；运营者有【强制取消跟场】【手动修改跟场状态】 | `pages/dashboard/` | 用例13/14/15 |
| C8 定时巡检云函数 | `cloudfunctions/TimerChecker/`（新建，config.json 配 cron 每小时） | 状态机 |
| C4 归档云函数 | `cloudfunctions/ArchiveManager/`（新建） | 用例11/12 |
| B8 归档页：比分/胜负必填 + 云盘链接选填 | `pages/archive/` | 用例11/12 |

**云函数 action 清单**：`getDashboard`、`forceCancelDuty`、`manualResetStatus`（DashboardManager）；`submitArchive`（ArchiveManager）。

**TimerChecker 定时规则**（需求文档用例7/11）：
- 距开赛 <48h 且仍为 pending → 强制置 `help`（红）；
- 到达预定结束时间 → 置 `settle`（橙）。
（云函数目录下 `config.json` 写 `{"triggers":[{"name":"hourly","type":"timer","config":"0 0 * * * * *"}]}`，部署后在控制台能看到定时器）

**归档规则**：submitArchive 校验比分+胜负非空 → 写 `ArchiveCollection` → 把比赛 `cellStatus` 置为已销号（从看板查询中排除）。

### 收尾阶段（主线完成后一起做）

- C6 `CalendarManager`（getCalendar/getMatchDetail/getMediaLink）+ 首页日历数据联通；
- C7 `TeamManager`（getTeamList/getTeamMatches）+ `pages/team/` 队伍栏目页（用例19）；
- 游客「看板」Tab 隐藏（自定义 tabBar 或入口权限判断）；
- 全流程联调 + 真机预览。

## 五、开发流程约定（重要）

1. **各自分支**：`feat/schedule`（A）/ `feat/duty`（B）/ `feat/dashboard`（C），完成后 PR 合并 `main`；
2. **开工 pull，收工 push**；
3. **云函数改了必须部署**（右键→上传并部署：云端安装依赖），群里说一声，否则云端跑的还是旧版；
4. **测试数据**用自己名字开头（`测试A-xxx`），别删别人的；
5. 前端问题点「编译」看模拟器，云端问题去云开发控制台看日志；
6. 卡住了先查 `../需求文档.md` 对应用例的步骤描述——分析模型就是伪代码。

## 六、数据库字段约定（三线共用，先约定避免返工）

```
MatchCollection（A 负责写入，B/C 负责读取）
  _id, teamId, teamName, sport, rival,
  matchTime(时间戳ms), endTime(时间戳ms), location,
  demands: ["饮用水","记分","摄影"],
  isTbd: bool,
  cellStatus: pending/confirmed/help/settle/tbd/cancelled/dutyCancelled,
  captainOpenid, createdAt, updatedAt

UserCollection（AuthManager 已定）
  _id, openid, role: captain/member/admin, nickname, teamId, bindAt

DutyRecordCollection（B 写入，C 读取）
  _id, matchId, matchTime, teamId,
  openid, nickname,
  type: confirm/decline/rescue/cancel/assign,
  createdAt

ArchiveCollection（C 写入）
  _id, matchId, teamId, teamName, sport, rival,
  score, result, mediaLink,
  submitterOpenid, createdAt

ActivationCodeCollection（AuthManager 已定）
  _id, code, role, nickname, teamId, used, expireAt
```

**字段就是接口**：A 先按此建赛程数据，B/C 按此读取。要改字段先在群里同步，改 `utils/status.js` 和本文件。

## 七、新队友上手 checklist

- [ ] 被添加为小程序项目成员（找发起者）
- [ ] `git clone` → 微信开发者工具导入项目 → 扫码登录
- [ ] 读 `../需求文档.md` 的分析模型（重点读自己负责的用例）
- [ ] 部署自己的云函数骨架 → 编译通过 → 在「我的」页用测试激活码绑定身份体验一遍
- [ ] 建自己的分支开工
