# 接口约定文档（v2 定稿）

> 三人共同遵守的**代码层接口契约**，唯一权威版本。目录基线：`yaliquankai/`（dev 分支）。
> 规则：要改任何约定，先群里讨论、更新本文、再改代码。与本文件冲突的代码以本文为准。
> 数据库字段约定见 `分工.md` 第六节（继续有效）；本文管代码怎么对接。

---

## 一、云函数返回协议（所有云函数强制，最高优先级）

### 1.1 返回格式

每个云函数的每次调用，`return` 的对象**必须**是以下两种形状之一：

```javascript
// 成功
return { code: 0, data: <业务数据，任意 JSON> };

// 失败
return { code: <非0>, message: <中文错误提示，可直接展示给用户> };
```

### 1.2 错误码表（全项目统一）

| code | 含义 | 典型场景 |
|---|---|---|
| 0 | 成功 | — |
| 400 | 参数错误 | 缺 matchId、必填字段为空 |
| 401 | 凭证无效或未绑定身份 | 游客调用需要身份的接口 |
| 403 | 已绑定但角色/归属不够 | 部员调队长接口、A 队队长改 B 队赛程 |
| 404 | 目标数据不存在 | matchId 查不到比赛 |
| 409 | 业务状态不允许 | 比赛已取消还想确认跟场、非 settle 状态想归档 |
| 500 | 服务器内部错误 | 云函数自身异常（catch 兜底返回） |

### 1.3 标准骨架（新建云函数照抄）

```javascript
const cloud = require("wx-server-sdk");
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

const ok = (data) => ({ code: 0, data });
const fail = (code, message) => ({ code, message });

exports.main = async (event) => {
  const { OPENID } = cloud.getWXContext();
  const { action } = event;
  console.log("[函数名]", action, event);   // 入口打日志，方便控制台排错
  try {
    switch (action) {
      case "xxx": return await xxx(OPENID, event);
      default: return fail(400, "未知操作");
    }
  } catch (e) {
    console.error("[函数名]", e);
    return fail(500, "服务器开小差了");
  }
};
```

参考实现：`cloudfunctions/AuthManager/index.js`（已部署验证）。

每个云函数目录必须含 `package.json`（抄 AuthManager 的，改 name 字段）。

---

## 二、前端调用封装（utils/call.js）

### 2.1 调用方式

页面**禁止**直接写 `wx.cloud.callFunction`，一律：

```javascript
const { call, getUser } = require("../../utils/call");

const result = await call("DutyManager", { action: "confirmDuty", matchId });
// 成功：result 即云函数返回的 data 本身（call 已拆封）
// 失败：自动 toast 云端 message，流程跳进 catch——页面 catch 里不用重复写提示
```

### 2.2 封装行为约定

- `code === 0` → resolve `data`；
- `code 非 0` → 自动 `wx.showToast(message)` 并 reject；
- 网络异常 → toast「网络异常，请重试」并 reject；
- 调用期间自动 showLoading / hideLoading；
- 云环境未配置时前置拦截（不发无效请求，直接报本地错误）——待实现项。

### 2.3 获取当前身份

```javascript
const { getUser, waitForUser } = require("../../utils/call");
const user = getUser();   // → { role, nickname, teamId } 或 { role: "guest" }
await waitForUser();      // 页面需要等待启动登录完成时使用
```

身份来源：`app.js` 启动时静默调用 `AuthManager.autoLogin`，缓存于 `globalData.userInfo`。

---

## 三、全局状态形状（app.js globalData）

```javascript
globalData: {
  env: <环境ID>,           // 实际值由 config/env.js 提供（见第五节）
  userInfo: null,          // 当前身份 { role, nickname, teamId }；游客 { role:"guest" }
  cloudState: "unconfigured" | "unsupported" | "ready",   // 云环境状态（调试用）
  envVersion: "develop" | "trial" | "release",            // 当前运行版本（调试用）
}
```

**身份字段定名 `userInfo`**（不是 user），读取身份只用 `getUser()` / `waitForUser()`；绑定成功后由 `setUser()` 更新缓存，禁止页面直读写 `globalData`。

---

## 四、状态常量模块（utils/status.js）

`miniprogram/utils/status.js` 是小程序前端的唯一状态定义来源，前端禁止写裸字符串（`"pending"` 这类）。云函数部署包不能直接引用小程序目录，实现时须在函数入口声明同值的冻结常量；状态值仍以本节为最终契约。

### 4.1 导出内容（定稿）

```javascript
CELL_STATUS   // 7 种单元格状态：pending/confirmed/help/settle/tbd/cancelled/dutyCancelled
ROLE          // 4 种角色：guest/captain/member/admin
DUTY_TYPE     // 4 种跟场记录：confirm/decline/rescue/assign；取消跟场记为 decline
STATUS_META   // 状态 → { color, label, desc } 显示配置
ROLE_META     // 角色 → { label, desc }
HOURS         // { FORCE_RED: 48 } 时间阈值
```

### 4.2 状态 → 显示 → 写入权（定稿）

| 值 | 含义 | 色值 | 谁可写入 |
|---|---|---|---|
| `pending` | 黄·待确认 | #F5A623 | A（新建/修改重置）、B（取消跟场回退） |
| `confirmed` | 绿·已确认 | #33C275 | B（确认/救场）、C（手动指派） |
| `help` | 红·求助中 | #E64340 | B（全员没空）、A（临期改期）、C（TimerChecker 拉红） |
| `settle` | 橙·待结算 | #FF8C00 | C（TimerChecker 到点转橙） |
| `tbd` | 灰·时间待定 | #999999 | A（勾选 TBD） |
| `cancelled` | 已取消 | #CCCCCC | A（取消比赛） |
| `dutyCancelled` | 无需跟场 | #BBBBBB | C（运营者强制取消跟场） |

现有 `status.js` 已使用 `Object.freeze` 冻结常量及展示元数据。

---

## 五、云环境接入（config/env.js）

采用三环境映射结构（已落地于 `miniprogram/config/env.js`）：

```javascript
const ENV_IDS = Object.freeze({
  develop: "yaliquankai-d2g2kt52247d144a8",   // 开发版（已填，云端就绪）
  trial: "",    // 体验版环境（将来发布前填）
  release: ""   // 正式版环境（将来发布前填）
});
```

- `app.js` 初始化改为 `wx.cloud.init({ env: envConfig.getCloudEnvId(), ... })`；
- 保留启动静默登录（autoLogin）；
- 云未配置时降级为本地骨架模式（不调云函数，`cloudState: "unconfigured"`）。
- `trial` / `release` 未配置时禁止回退到开发环境，调用层直接返回 503，避免体验版或正式版误写开发数据。

---

## 六、云函数清单与 action 归属

| 云函数 | 负责人 | action |
|---|---|---|
| `AuthManager` | ✅ 已完成 | `autoLogin`、`bindIdentity` |
| `ScheduleManager` | A | `getMyMatches`、`getMatchForEdit`、`saveMatch`、`getShareCard`、`cancelMatch` |
| `DutyManager` | ✅ B 已完成 | `getRespondPage`、`getRescuePage`、`getTeamStats`、`confirmDuty`、`declineDuty`、`generateHelpCard`、`rescueDuty`、`cancelMyDuty`、`getMyDuties` |
| `DashboardManager` | C | `getDashboard`、`forceCancelDuty`、`manualResetStatus` |
| `ArchiveManager` | C | `submitArchive` |
| `TimerChecker` | C | 定时触发（每小时 cron），无 action |
| `CalendarManager` | A（首版）/ 收尾 | A 先完成 `getCalendar`、`getMatchDetail`；收尾阶段补 `getMediaLink` |
| `TeamManager` | 收尾 | `getTeamList`、`getTeamMatches` |

规则：
- action 命名：驼峰、动词开头；
- **新增 action 先在第八节登记再写代码**；
- 云函数之间互不调用，共享数据走数据库；
- 改云函数必须重新部署（右键→上传并部署）并在群里通报。

---

## 七、页面路由与跳转参数

| 页面 | 进入方式 | 参数 |
|---|---|---|
| `pages/respond/index` | 微信赛程卡片直达 | `matchId` |
| `pages/rescue/index` | 微信求助卡片直达 | `matchId` |
| `pages/archive/index` | 橙色单元格点击 | `matchId` |
| `pages/schedule-form/index` | 首页队长入口 | `matchId`（修改模式带，新增不带） |
| 四 Tab 页 | `wx.switchTab` | 无参（switchTab 不支持带参） |

**分享卡片路径格式（定死）**：
- 赛程卡片：`/pages/respond/index?matchId=${match._id}`
- 求助卡片：`/pages/rescue/index?matchId=${match._id}`

**队长赛程管理入口（定稿）**：
- 首页在等待 `waitForUser()` 后，仅对 `captain` 显示「发布赛程」和「管理我的赛程」入口；游客看到的公开赛程内容不因角色变化；
- 新建直接进入不带参数的 `schedule-form`；管理列表调用 `ScheduleManager.getMyMatches`；点击可编辑比赛时携带 `matchId` 进入表单，再调用 `getMatchForEdit` 回填原始字段；
- 公开接口的 `MatchDTO` 不能代替编辑 DTO，表单禁止从公开卡片拼装原始提交数据。
**分享行为随状态（定稿）**：respond 页右上角分享（onShareAppMessage）按当前 cellStatus 返回：
- 红态（help）→ 求助卡片：标题【跟场求助】+ rescue 路径；
- 黄态/绿态（pending/confirmed）→ 赛程卡片：标题【跟场确认】+ respond 路径（仍指回响应页，非救场页）；
- 已完结（settle 及之后）不提供分享。

页面跳转规则：Tab 页之间用 `switchTab`；Tab→功能页用 `navigateTo`；返回用 `navigateBack`。

---

## 八、action 级接口登记表（先登记后编码，改动必须回写）

| 函数.action | 入参 | 出参 data | 错误情形 |
|---|---|---|---|
| AuthManager.autoLogin | 无（openid 自动注入） | `{ role, nickname?, teamId? }`，游客 `{ role:"guest" }` | 500 |
| AuthManager.bindIdentity | `{ code }` | `{ role, nickname, teamId }` | 400 未输入 / 401 码无效已用过期 / 403 已绑定 / 500 |
| DutyManager.getRespondPage | `{ matchId }` | `{ match: MatchDTO, myStatus: "none"|"confirmed"|"declined", stats: [{ nickname, count }], remainingCount, canHelp }` | 401 / 403 非部员（member 需本队；admin 全场景）/ 404 |
| DutyManager.getRescuePage | `{ matchId }` | `{ match: MatchDTO, myStatus: "none"|"confirmed" }` | 401 / 403 非部员 / 404 |
| DutyManager.confirmDuty | `{ matchId }` | `{ cellStatus }` | 401 / 403 非部员（member 非本队）/ 409 状态不允许 / 409 已开赛 |
| DutyManager.declineDuty | `{ matchId }` | `{ cellStatus, remainingCount, canHelp }` | 401 / 403 非部员（member 非本队）/ 409 非黄态 / 409 已开赛 |
| DutyManager.generateHelpCard | `{ matchId }` | `{ title, path }` | 401 / 403 非部员（member 需本队；admin 全场景）/ 404 / 409 非求助状态 |
| DutyManager.rescueDuty | `{ matchId }` | `{ cellStatus }` | 401 / 403 非部员 / 409 非求助状态 / 409 已开赛 |
| DutyManager.cancelMyDuty | `{ matchId }` | `{ cellStatus, canHelp }`（规则见下方取消三分支） | 401 / 403 非部员 / 404 无确认记录 / 409 / 409 已开赛 |
| DutyManager.getTeamStats | 无（取本人队伍） | `{ stats: [{ nickname, count }] }` | 401 / 403 非部员 / 400 无队伍 |
| DutyManager.getMyDuties | 无（openid 自动注入） | `{ list: [MatchDTO] }`——我确认/救场的、未完结的比赛（cellStatus 为 confirmed 或 settle），按 matchTime 正序；归档销号的除外 | 401 游客 / 500 |
| ScheduleManager.getMyMatches | 无（openid 自动注入） | `{ list: [ScheduleSummaryDTO] }`——本队未归档比赛，含已取消只读项，按 `updatedAt` 倒序 | 401 / 403 非队长 / 500 |
| ScheduleManager.getMatchForEdit | `{ matchId }` | `{ match: ScheduleEditDTO }`，含表单原始字段与 `version`，不含任何 openid | 403 非本队队长 / 404 |
| ScheduleManager.saveMatch | `{ matchId?, requestId, version?, sport, rival, matchTime?, endTime?, location, demands[], isTbd }`；新增不带 `matchId/version`，修改必须带二者 | `{ matchId, cellStatus, version }`（含重置/拉红后的状态） | 400 缺字段或时间非法 / 403 非本队队长 / 409 状态、版本或幂等冲突 |
| ScheduleManager.getShareCard | `{ matchId }` | `{ title, path }` | 403 非本队队长 / 404 / 409 状态不允许分享 |
| ScheduleManager.cancelMatch | `{ matchId, version }` | `{ cellStatus: "cancelled", version }`；重复取消幂等成功 | 403 非本队队长 / 404 / 409 已归档、已结束或版本冲突 |
| ArchiveManager.submitArchive | `{ matchId, score, result, mediaLink? }` | `{ archiveId }` | 400 必填空 / 403 非该场跟场人 / 409 非settle |
| DashboardManager.getDashboard | 无 | `{ teams: [{ teamId, teamName, matches: [Match记录] }] }`（未销号） | — |
| DashboardManager.forceCancelDuty | `{ matchId }` | `{ cellStatus: "dutyCancelled" }` | 403 非admin |
| DashboardManager.manualResetStatus | `{ matchId, target: "pending"|"confirmed", assignOpenid? }` | `{ cellStatus }` | 400 / 403 |
| CalendarManager.getCalendar | `{ fromTs, toTs }` | `{ matches: [MatchDTO], tbdMatches: [MatchDTO] }`；`matches` 含范围内已取消标记及已归档比分，`tbdMatches` 为未取消、未归档的时间待定比赛 | 400 时间范围非法 / 500 |
| CalendarManager.getMatchDetail | `{ matchId }` | `{ match: MatchDTO }`；已归档时合并比分、结果及 `hasMedia`，不直接返回 openid | 404 / 500 |
| CalendarManager.getMediaLink | `{ matchId }` | `{ mediaLink }`，未归档或未填写时为空字符串 | 404 / 500 |
| TeamManager.getTeamList | 无 | `[{ teamId, teamName, sport }]` | — |
| TeamManager.getTeamMatches | `{ teamId }` | `{ upcoming: [...], archived: [...] }` | — |

**通用约定**：
- `MatchDTO` 是脱敏展示对象，包含比赛基础字段、`timeText`、`demandsText`、`confirmerNickname?`，绝不返回任何用户 openid；
- `ScheduleSummaryDTO` 是队长管理列表对象，包含 `_id/teamName/sport/rival/timeText/location/isTbd/cellStatus/updatedAt/version`；
- `ScheduleEditDTO` 是仅向本队队长返回的编辑对象，包含 `_id/teamId/teamName/sport/rival/matchTime/endTime/location/demands/isTbd/cellStatus/version`；
- `timeText`（如「12月28日 15:30」）一律由**云端按 `Asia/Shanghai` 时区格式化**后返回，前端不重复实现；TBD 返回「时间待定」；
- `canHelp` 表示当前调用者是否可生成求助卡片；
- 入参里的 openid 一律不传（云端自动注入，前端传了也会被忽略）。

### 8.1 赛程保存与分享规则（A 线定稿）

**字段来源与校验**：
- `teamId`、`teamName`、`captainOpenid` 一律从 `OPENID -> UserCollection -> TeamCollection` 推导；前端不提交这些字段。`sport` 必填，并由云端校验为非空字符串；
- `location`、`rival` 必填，`demands` 必须是去重后的字符串数组；是否限制为预设选项由前端负责展示，云端仍要拒绝非字符串项；
- `isTbd=false` 时，`matchTime/endTime` 必须是毫秒时间戳，满足 `now < matchTime < endTime`；`isTbd=true` 时二者统一存为 `null`，TimerChecker 和普通日期范围查询跳过该比赛；
- 临期判断统一为 `0 < matchTime - now < 48 * 60 * 60 * 1000`；恰好 48 小时仍为黄色，越过边界后由读时重算或 TimerChecker 拉红。

**新建、修改与状态守卫**：
- 新建：`dutyRevision=1`、`version=1`；TBD 为 `tbd`，普通赛程按统一重算规则初始化（`>=48h` 为 `pending`，`<48h` 为 `help`）；
- 仅 `pending/confirmed/help/tbd` 允许编辑。`settle/cancelled/dutyCancelled` 或 `isArchived=true` 返回 409；
- 修改时间、地点或切换 TBD 均属于赛程基准变化：事务内递增 `dutyRevision`，清空 `confirmerOpenid/confirmerNickname/confirmerType`，旧 DutyRecord 保留；变为 TBD 直接置 `tbd`，变为确定时间后按统一重算规则得到 `pending/help`；
- 仅修改 `sport/rival/demands` 不递增 revision、不清跟场快照、不改当前状态；仅后勤需求发生变化时，前端按需求文档弹私聊提醒；
- 取消允许从 `pending/confirmed/help/tbd/dutyCancelled` 进入 `cancelled`，清空全部跟场快照且不删除 DutyRecord；已取消的重复请求幂等返回成功，`settle`/已归档禁止取消。

**并发与幂等**：
- `version` 是 A 线赛程管理操作的整数乐观锁；每次成功修改或取消递增 1，写入事务必须同时校验客户端提交的旧 `version`，不匹配返回 409 并要求刷新；B/C 的跟场状态流转不递增该 version；
- `requestId` 由表单在一次保存意图开始时生成，在网络重试期间保持不变。Match 保存 `lastRequestId`；云端先判断 requestId、再校验 version，相同比赛上的相同 `requestId` 直接返回上次结果，不重复递增 revision/version；
- 新建另存 `createRequestId`，云端先按它查重；部署前须为 `MatchCollection.createRequestId` 建唯一索引，防止并发重试创建重复比赛。成功或明确失败后，下一次人工保存才生成新的 `requestId`。
- `cancelMatch` 先判断比赛是否已经是 `cancelled`，再校验 version；因此首次取消成功但响应丢失时，携带旧 version 的重试仍幂等成功。

**分享实现**：
- `getShareCard` 仅允许本队队长为 `pending/confirmed/help/tbd` 比赛调用，路径固定指向 RespondPage；TBD 标题明确包含「时间待定」；
- 表单页在保存成功、进入编辑页或修改成功后预取并缓存 `{ title, path }`；分享按钮使用 `open-type="share"`，`onShareAppMessage` 同步返回缓存配置；
- 缓存尚未完成时禁用分享按钮；禁止在 `onShareAppMessage` 中临时 `await` 云函数，也不设计“云函数返回后由代码主动拉起分享面板”的流程。
**取消语义（定稿）：取消确认 ≡ 本人最新表态变为「没空」**：
- 部员取消跟场确认或取消救场，云端一律：写 DutyRecord(decline)（非 cancel——语义上他现在没空）+ 清 confirmerOpenid，然后**统一走重算**；
- 因此「取消后看板什么颜色」没有写死的答案，由重算规则决定（见下）。

**统一重算规则 recalcCellStatus（所有表态变动的唯一颜色裁决器，定稿）**：
任何 confirmDuty / declineDuty / cancelMyDuty / rescueDuty / TimerChecker 巡检 / saveMatch 清空表态之后，云端统一调用本函数重算 `cellStatus`：

```javascript
// 伪代码：tbd / cancelled / dutyCancelled / settle 为挂起或终态，不参与重算
if (match.confirmerOpenid)            return "confirmed"; // 有人确认 → 绿
if (已decline人数 >= 本队经理人总数)    return "help";      // 全员没空 → 红
if (match.matchTime - now < 48h)      return "help";      // 临期无人确认 → 红
return "pending";                                                            // 其余 → 黄
```

- 红态（help）自动解锁求助卡片；**`canHelp = (cellStatus === "help" && 调用者为部员)`**（member 需本队，admin 全场景）——红态下本队经理人（含 admin）皆可生成转发求助卡片；
- 「第一责任人」仅为**文案指称**，不参与权限判定：拉红时对触发者（最后没空者/取消者）弹强提醒；48h 无人表态拉红无触发者时，任何经理人打开页面见温和提示；
- 典型推论（不再是硬编码，而是重算的自然结果）：三人队甲乙已没空、丙取消 → 全员没空 → 红；单人队取消 → 红；多人队还有未表态者且 >48h 取消 → 黄；救场者取消（其确认被撤，其余人早已 decline）→ 红。
- 所有表态变动（含取消）必须与 DutyRecord 留痕同事务写入（见 8.2）。

**读时重算（定稿）**：getRespondPage / getRescuePage 在返回数据前**先执行 recalcCellStatus** 并写回最新状态——消除 TimerChecker 每小时巡检之间的最多 1 小时状态延迟窗口，用户进入页面见到的必为正确颜色。

**状态与操作守卫（定稿，云函数必须校验 + 前端按钮条件渲染）**：
- **确认跟场：黄态、红态均可**（部员任何时刻可回补，红态确认即绿；member 限本队，admin 全场景）；绿态（已有人确认）下除 confirmer 本人重复点击（幂等）外返回 409；
- **没空：仅黄态可点**，其余状态返回 409（红态下表态没空无信息增量，前端隐藏该按钮）；
- **取消/救场均设已开赛硬禁**：`matchTime <= now` 时 confirmDuty / declineDuty / rescueDuty / cancelMyDuty 四动作一律 409「比赛已开始」——比赛开打后任何表态写入都无意义且会污染状态机（settle 归档线依赖 endTime，不受此闸影响）；前端对应按钮同步隐藏；
- 由重算规则推论：黄态必然处于 48h 线之前，黄态表态无「临期」场景；
- 红态是**双通道**：本队经理人（含 admin 全场景）可在响应页直接确认；任何部员可经大群求助卡片进救场页接单。两通道并发由 8.2 条件更新幂等兜底（先到先得，后到 409「已有人跟场」）；救场页在被确认后展示「本场已有人跟场」；
- **admin（运营者）同时具备部员全部能力**：可对任意队比赛确认/没空/取消/救场，不受 teamId 限制（`requireTeamManager` 对 admin 放行）；`cancelMyDuty` 仍以 `confirmerOpenid === openid` 限定只能撤自己确认的场。
- 安全锁（强提醒二次确认）只存在于两个动作：① 绿态 + <48h 的 cancelMyDuty（取消后必红）；② 红态确认人的 onCancelRescue（取消后必回红）。declineDuty 不需要安全锁。

### 8.2 数据一致性与并发规则

- 所有写操作先由云函数根据 `OPENID` 查询 `UserCollection` 校验角色和归属，不信任前端传入的角色、teamId 或 openid；
- **事务取舍（实现定稿）**：云数据库事务不支持 `where()`/`count()`，而重算器 `recalcCellStatus` 依赖 count 聚合，无法整体入事务。故采用「**条件更新 + 唯一索引 upsert**」近似保证（理由详见 `DutyManager` 头部注释）：
  - 抢占名额 / 撤确认：`where({ _id, cellStatus / confirmerOpenid }).update()` 条件更新，`.updated === 0` 即被抢先 → 409 / 404；
  - 表态留痕：`DutyRecordCollection` 建 `matchId + openid` 联合**唯一索引**，`upsert` 收敛为一条，天然幂等；
  - 颜色裁决：`recalcCellStatus` 以**库内实时 `confirmerOpenid`** 为准（不信任调用方内存快照），配合读时重算兜底自愈；
- `MatchCollection` 保存当前跟场人快照；`DutyRecordCollection` 作为**留痕表**（一人一场至多一条，`type` 随时间覆盖更新、`updatedAt` 留审计轨迹），不再是纯追加日志；
- 重复点击、网络重试和并发接单必须幂等：同一用户不能产生重复有效确认，只有一个救场人能从 `help` 成功迁移到 `confirmed`；
- **表态幂等（定稿）**：同一用户对同一场比赛的 confirm / decline / rescue 均为「更新本人最新表态」而非新增记录——重复点「没空」不得累加 decline 计数，重复点「确认」幂等返回成功，重复接单只有首个成功；
- **`dutyRevision` 建议（dev 合并后）**：`MatchCollection` 上的 `dutyRevision` / `version` / `createRequestId` / `lastRequestId` 为 A 线契约字段，其中 `version` / `createRequestId` / `lastRequestId` 用于 A 线赛程幂等与乐观锁（照 8.1 实现）。关于 `dutyRevision`：**建议 A 线不做 revision 分代**——`saveMatch` 改时间/地点/切 TBD 需要清空确认时，直接清 `MatchCollection` 跟场人快照 + 调统一重算器 `recalcCellStatus` 即可（不递增 revision、不把 `DutyRecordCollection` 改成只追加事件日志），与 B 线「一人一场至多一条 upsert」现状对齐、无需改造留痕表；最终是否分代由 A 线确认。
- 对游客开放的接口必须返回脱敏 DTO，禁止暴露 `captainOpenid`、`confirmerOpenid`、`submitterOpenid`。

### 8.3 三线开发对齐说明（dev 合并后 · 对 A/C 线为建议）

> 本节以已实装的 B 线代码为事实基准，给出对 A/C 线开发的**建议对接方式**；最终以 A/C 线负责同学确认的契约为准。

**共同约定（B 线现状，建议遵循）**：
- `DUTY_TYPE` 定稿为 **4 种**：`confirm / decline / rescue / assign`；**取消跟场一律写 `decline`，不存在 `cancel` 类型**；
- `DutyManager` 现为 **9 个 action**（含云端 `generateHelpCard`），改动后需重新上传部署；
- 前端 mock 数据结构为 `_teamSize / _declinedCount` 版（4 套数据，含「测试B-超时」），与 `utils/recalc.js` 的 `mockRecalc` 配套；
- `match-detail-card` 组件按 MatchDTO 传参（`timeText/demandsText` 已由云端格式化），事件名为 `cancelduty / forcecancel / manualreset / toggle`。

**给 A 线（赛程线，建议）**：
- `ScheduleManager` action 清单：`getMyMatches` / `getMatchForEdit` / `saveMatch` / `getShareCard` / `cancelMatch`；
- 新建赛程状态初始化建议：TBD→`tbd`；普通 `>=48h`→`pending`、`<48h`→`help`；`requestId` 幂等 + `version` 乐观锁照 8.1 实现；
- **建议清空确认不引入 `dutyRevision` 分代**：改时间/地点/切 TBD 时清 `MatchCollection` 快照 + 调 `recalcCellStatus`（见 8.2 建议）。

**给 C 线（看板/归档线，建议）**：
- 建议读数据按「`MatchCollection` 快照（`confirmerOpenid/confirmerNickname/cellStatus`）+ `DutyRecordCollection` 一人一场一条（type 覆盖更新）」来读，不建议按"只追加 + revision"旧契约读；
- `TimerChecker` 的拉红/转橙规则建议与 B 线 `recalcCellStatus` 同规则。

---

## 九、前端其他统一约定

1. **组件**：业务卡片统一用全局注册的 `match-detail-card`；占位页统一用已注册的 `page-placeholder`；
2. **app.json**：tabBar 四 Tab 配置为定稿（现有版本即最终版）；主题色定 **#7a1733（新雅紫红）**、白字导航栏——各页面 wxss 引用主题色一律用该值，不得自创；
3. **require 路径**：页面/组件内引 utils 用 `../../utils/xxx`，引 config 用 `../../config/env`；
4. **禁止事项**：前端直连数据库（物理已禁）、页面裸调 wx.cloud.callFunction、代码里写状态/角色裸字符串、页面直读 `globalData`（用 `getUser()`）；
5. **Git**：分支 `main`（稳定）/ `dev`（集成）/ `feat/schedule`（A）/ `feat/duty`（B）/ `feat/dashboard`（C）；提交前缀 `feat:` / `fix:` / `chore:` / `docs:`。

---

## 十、基线落地状态

- [x] 创建 `miniprogram/config/env.js`，app.js 按运行版本初始化，删除 `envList.js`；
- [x] `utils/status.js` 冻结常量并核对状态色；
- [x] `utils/call.js` 增加云环境前置拦截；
- [x] 收编并全局注册 `page-placeholder`；
- [x] 导航栏与现有业务样式统一为 #7a1733；
- [x] 微信开发者工具编译；
- [x] 「我的」页绑定流程回归测试；
- [x] 确认云端第六个集合 `TeamCollection` 及索引后通知全员拉取。

**B·跟场线落地状态（DutyManager 实装）**：
- [x] DutyManager 9 个 action 全部实装（getRespondPage/getRescuePage/getTeamStats/confirmDuty/declineDuty/generateHelpCard/rescueDuty/cancelMyDuty/getMyDuties），含统一重算器 recalcCellStatus、表态幂等 upsert、读时重算、已开赛硬禁、toDTO 脱敏；
- [x] respond / rescue / profile（B5b 我的跟场）三页真实分支接通（USE_MOCK=false）；
- [x] 并发安全：confirmDuty / rescueDuty / cancelMyDuty 三处统一条件更新，recalcCellStatus 以库内实时 confirmerOpenid 为准；
- [x] admin（运营者）全场景跟场放行（`requireTeamManager` 对 admin 不校验 teamId）；
- [x] `DutyRecordCollection` 已建 `matchId+openid` 联合唯一索引。
